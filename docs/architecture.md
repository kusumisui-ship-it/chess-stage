# Architecture

The offline build is a dependency-free static web application. The chess engine and UI are separated into ordered classic scripts so GitHub Pages can serve the project without a build step.
